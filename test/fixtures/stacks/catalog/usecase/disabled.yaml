components:
  terraform:
    vpc/disabled:
      metadata:
        component: target
      vars:
        name: "vpc-terraform"
        enabled: false
        subnet_type_tag_key: "eg.cptest.co/subnet/type"
